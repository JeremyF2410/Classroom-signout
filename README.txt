CLASSROOM SIGN-OUT — TIMER + HEADER FIX

Upload these files to the root of your GitHub repository:
- index.html
- styles.css
- app.js
- manifest.json
- service-worker.js
- icon.png

Fixes in this version:
- A large live “X min” counter is shown on every Currently Out card.
- Destination is displayed separately below the timer.
- Currently Out uses three columns so all 26 students fit comfortably.
- The header and summary rows are taller.
- “In class”, “Currently out” and “Trips today” labels are vertically centred and cannot spill outside their cards.

After uploading, open:
https://jeremyf2410.github.io/Classroom-signout/?timerfix=4

If using a Home Screen shortcut, delete the old shortcut, open the updated URL in Safari, confirm the new timer is visible, and add it to the Home Screen again.
